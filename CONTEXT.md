# 우리로그

커플·부부가 공동 생활비와 각자 예산을 함께 운영하는 가계부 서비스입니다.

이 문서는 **어떤 한국어 단어가 어느 개념을 가리키는지** 고정하는 용어집입니다.
도메인 모델 자체는 [`docs/engineering/domain-model.md`](./docs/engineering/domain-model.md)에 있습니다.

## 이 용어집의 범위

- 여기 정한 말은 **사용자에게 보이는 문구와 `docs/**`**의 기준입니다.
  Kotlin 엔티티명, DB 테이블·컬럼, API 필드명은 대상이 아닙니다.
- 각 항목에 코드 식별자를 병기한 것은 **찾아가기 위한 것**이며, 그 이름을 화면에 쓰라는 뜻이 아닙니다.
- V1 개념만 정본으로 올립니다. legacy 이름은 `_Avoid_` 줄에만 남깁니다.

새 말을 고를 때는 친근한 쪽을 택하되, 둘 중 하나에 걸리면 정확한 쪽을 택합니다.

1. 같은 화면에 함께 보이는 다른 개념과 헷갈리는 말
2. 사용자가 금액을 잘못 넣거나 잘못 읽을 수 있는 말

## 가계부

**가계부**:
거래·예산·카테고리·설정을 담는 그릇. 공동 가계부와 개인 가계부가 있습니다. (`Ledger`)
_Avoid_: 장부, 공간, 지갑

**공동 가계부**:
두 사람이 함께 쓰는 가계부.

**개인 가계부**:
나만 쓰는 가계부. 상대방에게 보이지 않습니다.

## 예산

**예산 기간**:
예산을 운영하는 한 구간. 시작일로 식별하며 달력 월과 다를 수 있습니다. (`BudgetPeriod`)
_Avoid_: 월, 이번 달, ledger month(legacy)

**이번 기간**:
지금 날짜가 속한 예산 기간.
_Avoid_: 이번 달 — 매월 15일 시작인 사용자에게 틀린 말이 됩니다

**전체 예산**:
한 예산 기간에 쓰기로 정한 총액. 나눈 금액의 합과 같지 않을 수 있습니다.
_Avoid_: 총예산, 월 예산

**예산 나누기**:
전체 예산을 공동 예산과 각자의 내 예산으로 가르는 일. (`BudgetAllocation`)
_Avoid_: 배분, 예산 배분, 할당, 몫

**공동 예산**:
두 사람이 함께 쓰는 몫. (`BudgetAllocation`, scope `SHARED`)

**내 예산**:
내가 혼자 쓰는 몫. (`BudgetAllocation`, scope `PERSONAL`)
_Avoid_: 내 할당 예산, 할당 예산

> 상대방의 몫을 가리킬 때만 `개인 예산` 또는 `{닉네임} 예산`을 씁니다.
> `내 예산`은 1인칭 전용입니다.

**카테고리 예산**:
공동 예산 또는 내 예산 안에서 카테고리 하나에 정한 금액. (`AllocationCategoryBudget`)
_Avoid_: 대분류 예산, 세부 예산

**예비비**:
전체 예산에서 아직 나누지 않고 남겨둔 금액. 지출로 바로 차감되지 않습니다.

**예비비 옮기기**:
예비비를 공동 예산이나 내 예산으로 보내는 일. (`ReserveTransfer`)

**주간 권장액**:
내 예산을 기준으로 한 주에 쓸 만한 금액의 안내값. 한도가 아닙니다. (`WeeklyBudgetGuide`)
_Avoid_: 주간 예산, 주간 예산 가이드, 주간 기준액

## 카테고리

**카테고리**:
지출·수입을 묶는 단위. 예산과 분석이 이 층을 기준으로 움직입니다. 예: 식비, 교통. (`LedgerCategoryGroup`)
_Avoid_: 대분류, 묶음, 분류

**세부 카테고리**:
카테고리 안의 항목. 거래에 실제로 붙는 값입니다. 예: 식비 안의 카페. (`LedgerCategory`)
_Avoid_: 소분류, 항목

> 이 말은 **설정 화면과 문서에서만** 씁니다. 거래를 적는 화면에서는 층 이름을 노출하지 않고,
> 카테고리를 고르면 그 안의 항목이 펼쳐지는 배치로 두 층을 구분합니다. 화면에 보이는 말은 `카테고리` 하나입니다.

> 한국어와 코드가 한 칸 어긋납니다. 한국어 `카테고리`는 코드의 `LedgerCategoryGroup`이고,
> 코드의 `category` / `LedgerCategory`는 한국어로 `세부 카테고리`입니다. 문구를 쓸 때 확인이 필요합니다.

## 거래

**거래**:
가계부에 적힌 지출·수입·이체 한 건. (`Transaction`)

**거래 내역**:
거래 목록 화면.
_Avoid_: 가계부(내비 라벨로 쓰지 않음), 기록

**나만 보기**:
공동 가계부에서 이 거래를 상대방에게 보이지 않게 하는 설정. (`Transaction.sharedWithPartner`)
_Avoid_: 비공개, 공개 — `공동`과 한 글자 차이라 예산 축과 헷갈립니다

**함께 보기**:
`나만 보기`의 반대. 이 거래가 상대방에게도 보이는 상태.

**보이는 범위**:
무엇이 상대방에게 보이고 무엇이 안 보이는지의 축. 예산이 어디서 차감되는지(`공동`/`개인`)와 다른 축입니다.
_Avoid_: 공개 범위

> `공개`는 인증 없이 접근하는 영역(공개 영역, 공개 초대 확인)이나 공개 환경 변수처럼
> 거래 노출과 무관한 곳에서만 씁니다.

**결제 수단**:
현금, 카드, 계좌이체, 기타.
_Avoid_: 결제수단(붙여쓰기)

**계좌이체**:
결제 수단의 한 값. `계좌`라는 말은 이체 관련 문구 안에서만 쓰고,
등록·저장되는 대상의 이름으로는 쓰지 않습니다. 우리로그는 계좌를 연동하지 않습니다.

## 자동 기록

**자동 기록**:
예정일이 되면 앱이 거래를 대신 만들어주는 것. 반복 지출과 할부가 여기 속합니다. (`ScheduledPlan`)
_Avoid_: 예약 거래, 반복 거래(상위 개념 이름으로 쓰지 않음), recurring transaction(legacy)

**반복 지출**:
매주·매월·매년 되풀이되는 지출. (`ScheduledPlan`, type `RECURRING_EXPENSE`)

**고정비**:
반복 지출 중 고정비로 표시한 것. 반복 지출·할부와 나란한 갈래가 **아니라** 반복 지출의 부분집합입니다.
(`ScheduledPlan.fixedExpense`)
_Avoid_: fixed budget(`FixedBudgetTemplate`은 다른 개념이며 화면에 노출하지 않습니다)

**할부**:
전체 원금을 여러 회차로 나눠 결제하는 것. (`ScheduledPlan`, type `INSTALLMENT`)

## 쓰지 않는 말

**정산**:
멤버 간 정산은 V1 제외 범위입니다. 화면 문구에 쓰지 않습니다.
공동 예산 지출을 설명할 때는 어디서 빠지고 누구에게 보이는지를 그대로 말합니다.
