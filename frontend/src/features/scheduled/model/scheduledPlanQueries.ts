import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { createRecurringExpensePlan, deleteScheduledPlan, getFixedExpenses, listScheduledPlans, pauseScheduledPlan, resumeScheduledPlan, updateScheduledPlan, type RecurringExpensePlanRequest, type UpdateScheduledPlanRequest } from '../api/scheduledPlanApi'
export const scheduledPlanQueryKeys = { all: ['scheduled-plans'] as const, list: (ledgerId: number) => ['scheduled-plans', ledgerId] as const, fixed: (ledgerId: number) => ['scheduled-plans', ledgerId, 'fixed'] as const }
export function useScheduledPlansQuery(ledgerId: number | undefined) { return useQuery({ queryKey: ledgerId ? scheduledPlanQueryKeys.list(ledgerId) : scheduledPlanQueryKeys.all, queryFn: () => listScheduledPlans(ledgerId!), enabled: Boolean(ledgerId), retry: false }) }
export function useFixedExpensesQuery(ledgerId: number | undefined) { return useQuery({ queryKey: ledgerId ? scheduledPlanQueryKeys.fixed(ledgerId) : scheduledPlanQueryKeys.all, queryFn: () => getFixedExpenses(ledgerId!), enabled: Boolean(ledgerId), retry: false }) }
export function useCreateRecurringExpensePlanMutation(ledgerId: number | undefined) { const client = useQueryClient(); return useMutation({ mutationFn: (request: RecurringExpensePlanRequest) => createRecurringExpensePlan(ledgerId!, request), onSuccess: () => client.invalidateQueries({ queryKey: scheduledPlanQueryKeys.all }) }) }
export function useUpdateScheduledPlanMutation() { const client = useQueryClient(); return useMutation({ mutationFn: ({ planId, ...request }: UpdateScheduledPlanRequest & { planId: number }) => updateScheduledPlan(planId, request), onSuccess: () => client.invalidateQueries({ queryKey: scheduledPlanQueryKeys.all }) }) }
export function useScheduledPlanActionMutation(action: 'pause' | 'delete' | 'resume') { const client = useQueryClient(); return useMutation({ mutationFn: async ({ planId, nextDueDate }: { planId: number; nextDueDate?: string }) => { if (action === 'pause') return pauseScheduledPlan(planId); if (action === 'resume') return resumeScheduledPlan(planId, nextDueDate!); await deleteScheduledPlan(planId); return undefined }, onSuccess: () => client.invalidateQueries({ queryKey: scheduledPlanQueryKeys.all }) }) }
